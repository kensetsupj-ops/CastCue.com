name: Reset Quotas Monthly

on:
  schedule:
    - cron: '0 0 1 * *'  # 毎月1日午前0時UTC
  workflow_dispatch:  # 手動実行も可能

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - name: Call reset quotas API
        run: |
          curl -X POST ${{ secrets.APP_URL }}/api/cron/reset-quotas
